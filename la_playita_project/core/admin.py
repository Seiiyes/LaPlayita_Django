# C:\laplayita\la_playita_project\core\admin.py
from django.contrib import admin

# No models are registered here anymore after refactoring.
# Each app should have its own admin.py to register its models.